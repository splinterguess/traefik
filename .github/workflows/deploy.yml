name: Deploy Traefik
on: [push]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Mise à jour et lancement
        run: |
          cd ~/traefik
          git pull origin main
          docker compose up -d
